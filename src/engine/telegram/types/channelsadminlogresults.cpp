// This file generated by libqtelegram-code-generator.
// You can download it from: https://github.com/Aseman-Land/libqtelegram-code-generator
// DO NOT EDIT THIS FILE BY HAND -- YOUR CHANGES WILL BE OVERWRITTEN

#include "channelsadminlogresults.h"
#include "core/inboundpkt.h"
#include "core/outboundpkt.h"
#include "../coretypes.h"

#include <QDataStream>

ChannelsAdminLogResults::ChannelsAdminLogResults(ChannelsAdminLogResultsClassType classType, InboundPkt *in) :
    m_classType(classType)
{
    if(in) fetch(in);
}

ChannelsAdminLogResults::ChannelsAdminLogResults(InboundPkt *in) :
    m_classType(typeChannelsAdminLogResults)
{
    fetch(in);
}

ChannelsAdminLogResults::ChannelsAdminLogResults(const ChannelsAdminLogResults &another) :
    TelegramTypeObject(),
    m_classType(typeChannelsAdminLogResults)
{
    operator=(another);
}

ChannelsAdminLogResults::ChannelsAdminLogResults(const Null &null) :
    TelegramTypeObject(null),
    m_classType(typeChannelsAdminLogResults)
{
}

ChannelsAdminLogResults::~ChannelsAdminLogResults() {

}

void ChannelsAdminLogResults::setChats(const QList<Chat> &chats) {
    m_chats = chats;
}

QList<Chat> ChannelsAdminLogResults::chats() const {
    return m_chats;
}

void ChannelsAdminLogResults::setEvents(const QList<ChannelAdminLogEvent> &events) {
    m_events = events;
}

QList<ChannelAdminLogEvent> ChannelsAdminLogResults::events() const {
    return m_events;
}

void ChannelsAdminLogResults::setUsers(const QList<User> &users) {
    m_users = users;
}

QList<User> ChannelsAdminLogResults::users() const {
    return m_users;
}

bool ChannelsAdminLogResults::operator ==(const ChannelsAdminLogResults &b) const {
    return m_classType == b.m_classType &&
           m_chats == b.m_chats &&
           m_events == b.m_events &&
           m_users == b.m_users;
}

ChannelsAdminLogResults &ChannelsAdminLogResults::operator =(const ChannelsAdminLogResults &b) {
    m_classType = b.m_classType;
    m_chats = b.m_chats;
    m_events = b.m_events;
    m_users = b.m_users;
    setNull(b.isNull());
    return *this;
}

void ChannelsAdminLogResults::setClassType(ChannelsAdminLogResults::ChannelsAdminLogResultsClassType classType) {
    m_classType = classType;
}

ChannelsAdminLogResults::ChannelsAdminLogResultsClassType ChannelsAdminLogResults::classType() const {
    return m_classType;
}

bool ChannelsAdminLogResults::fetch(InboundPkt *in) {
    LQTG_FETCH_LOG;
    int x = in->fetchInt();
    switch(x) {
    case typeChannelsAdminLogResults: {
        if(in->fetchInt() != (qint32)CoreTypes::typeVector) return false;
        qint32 m_events_length = in->fetchInt();
        m_events.clear();
        for (qint32 i = 0; i < m_events_length; i++) {
            ChannelAdminLogEvent type;
            type.fetch(in);
            m_events.append(type);
        }
        if(in->fetchInt() != (qint32)CoreTypes::typeVector) return false;
        qint32 m_chats_length = in->fetchInt();
        m_chats.clear();
        for (qint32 i = 0; i < m_chats_length; i++) {
            Chat type;
            type.fetch(in);
            m_chats.append(type);
        }
        if(in->fetchInt() != (qint32)CoreTypes::typeVector) return false;
        qint32 m_users_length = in->fetchInt();
        m_users.clear();
        for (qint32 i = 0; i < m_users_length; i++) {
            User type;
            type.fetch(in);
            m_users.append(type);
        }
        m_classType = static_cast<ChannelsAdminLogResultsClassType>(x);
        return true;
    }
        break;

    default:
        LQTG_FETCH_ASSERT;
        return false;
    }
}

bool ChannelsAdminLogResults::push(OutboundPkt *out) const {
    out->appendInt(m_classType);
    switch(m_classType) {
    case typeChannelsAdminLogResults: {
        out->appendInt(CoreTypes::typeVector);
        out->appendInt(m_events.count());
        for (qint32 i = 0; i < m_events.count(); i++) {
            m_events[i].push(out);
        }
        out->appendInt(CoreTypes::typeVector);
        out->appendInt(m_chats.count());
        for (qint32 i = 0; i < m_chats.count(); i++) {
            m_chats[i].push(out);
        }
        out->appendInt(CoreTypes::typeVector);
        out->appendInt(m_users.count());
        for (qint32 i = 0; i < m_users.count(); i++) {
            m_users[i].push(out);
        }
        return true;
    }
        break;

    default:
        return false;
    }
}

QMap<QString, QVariant> ChannelsAdminLogResults::toMap() const {
    QMap<QString, QVariant> result;
    switch(static_cast<int>(m_classType)) {
    case typeChannelsAdminLogResults: {
        result["classType"] = "ChannelsAdminLogResults::typeChannelsAdminLogResults";
        QList<QVariant> _events;
        //for(const ChannelAdminLogEvent &m__type: m_events)
        for(int i = 0; i < m_events.size(); ++i) {
            const ChannelAdminLogEvent &m__type = m_events[i];
            if( !m__type.isNull() ) _events << m__type.toMap();
        }
        result["events"] = _events;
        QList<QVariant> _chats;
        //for(const Chat &m__type: m_chats)
        for(int i = 0; i < m_chats.size(); ++i) {
            const Chat &m__type = m_chats[i];
            if( !m__type.isNull() ) _chats << m__type.toMap();
        }
        result["chats"] = _chats;
        QList<QVariant> _users;
        //for(const User &m__type: m_users)
        for(int i = 0; i < m_users.size(); ++i) {
            const User &m__type = m_users[i];
            if( !m__type.isNull() ) _users << m__type.toMap();
        }
        result["users"] = _users;
        return result;
    }
        break;

    default:
        return result;
    }
}

ChannelsAdminLogResults ChannelsAdminLogResults::fromMap(const QMap<QString, QVariant> &map) {
    ChannelsAdminLogResults result;
    if(map.value("classType").toString() == "ChannelsAdminLogResults::typeChannelsAdminLogResults") {
        result.setClassType(typeChannelsAdminLogResults);
        QList<QVariant> map_events = map["events"].toList();
        QList<ChannelAdminLogEvent> _events;
        //for(const QVariant &var: map_events)
        for(int i = 0; i < map_events.size(); ++i) {
            const QVariant &var = map_events[i];
            _events << ChannelAdminLogEvent::fromMap(var.toMap());
        }
        result.setEvents(_events);
        QList<QVariant> map_chats = map["chats"].toList();
        QList<Chat> _chats;
        //for(const QVariant &var: map_chats)
        for(int i = 0; i < map_chats.size(); ++i) {
            const QVariant &var = map_chats[i];
            _chats << Chat::fromMap(var.toMap());
        }
        result.setChats(_chats);
        QList<QVariant> map_users = map["users"].toList();
        QList<User> _users;
        //for(const QVariant &var: map_users)
        for(int i = 0; i < map_users.size(); ++i) {
            const QVariant &var = map_users[i];
            _users << User::fromMap(var.toMap());
        }
        result.setUsers(_users);
        return result;
    }
    return result;
}

ChannelsAdminLogResults ChannelsAdminLogResults::fromJson(const QString &json) {
    return ChannelsAdminLogResults::fromMap(QJsonDocument::fromJson(json.toUtf8()).toVariant().toMap());
}

QByteArray ChannelsAdminLogResults::getHash(QCryptographicHash::Algorithm alg) const {
    QByteArray data;
    QDataStream str(&data, QIODevice::WriteOnly);
    str << *this;
    return QCryptographicHash::hash(data, alg);
}

QDataStream &operator<<(QDataStream &stream, const ChannelsAdminLogResults &item) {
    stream << static_cast<uint>(item.classType());
    switch(item.classType()) {
    case ChannelsAdminLogResults::typeChannelsAdminLogResults:
        stream << item.events();
        stream << item.chats();
        stream << item.users();
        break;
    }
    return stream;
}

QDataStream &operator>>(QDataStream &stream, ChannelsAdminLogResults &item) {
    uint type = 0;
    stream >> type;
    item.setClassType(static_cast<ChannelsAdminLogResults::ChannelsAdminLogResultsClassType>(type));
    switch(type) {
    case ChannelsAdminLogResults::typeChannelsAdminLogResults: {
        QList<ChannelAdminLogEvent> m_events;
        stream >> m_events;
        item.setEvents(m_events);
        QList<Chat> m_chats;
        stream >> m_chats;
        item.setChats(m_chats);
        QList<User> m_users;
        stream >> m_users;
        item.setUsers(m_users);
    }
        break;
    }
    return stream;
}

/*QDebug operator<<(QDebug debug,  const ChannelsAdminLogResults &item) {
    QDebugStateSaver saver(debug);
    Q_UNUSED(saver)
    debug.nospace() << "Telegram.ChannelsAdminLogResults(";
    switch(item.classType()) {
    case ChannelsAdminLogResults::typeChannelsAdminLogResults:
        debug.nospace() << "classType: typeChannelsAdminLogResults";
        debug.nospace() << ", events: " << item.events();
        debug.nospace() << ", chats: " << item.chats();
        debug.nospace() << ", users: " << item.users();
        break;
    }
    debug.nospace() << ")";
    return debug;
}
*/
