// This file generated by libqtelegram-code-generator.
// You can download it from: https://github.com/Aseman-Land/libqtelegram-code-generator
// DO NOT EDIT THIS FILE BY HAND -- YOUR CHANGES WILL BE OVERWRITTEN

#include "stickerset.h"
#include "core/inboundpkt.h"
#include "core/outboundpkt.h"
#include "../coretypes.h"

#include <QDataStream>

StickerSet::StickerSet(StickerSetClassType classType, InboundPkt *in) :
    m_accessHash(0),
    m_count(0),
    m_flags(0),
    m_hash(0),
    m_id(0),
    m_classType(classType)
{
    if(in) fetch(in);
}

StickerSet::StickerSet(InboundPkt *in) :
    m_accessHash(0),
    m_count(0),
    m_flags(0),
    m_hash(0),
    m_id(0),
    m_classType(typeStickerSet)
{
    fetch(in);
}

StickerSet::StickerSet(const StickerSet &another) :
    TelegramTypeObject(),
    m_accessHash(0),
    m_count(0),
    m_flags(0),
    m_hash(0),
    m_id(0),
    m_classType(typeStickerSet)
{
    operator=(another);
}

StickerSet::StickerSet(const Null &null) :
    TelegramTypeObject(null),
    m_accessHash(0),
    m_count(0),
    m_flags(0),
    m_hash(0),
    m_id(0),
    m_classType(typeStickerSet)
{
}

StickerSet::~StickerSet() {

}

void StickerSet::setAccessHash(qint64 accessHash) {
    m_accessHash = accessHash;
}

qint64 StickerSet::accessHash() const {
    return m_accessHash;
}

void StickerSet::setArchived(bool archived) {
    if(archived) m_flags = (m_flags | (1<<1));
    else m_flags = (m_flags & ~(1<<1));
}

bool StickerSet::archived() const {
    return (m_flags & 1<<1);
}

void StickerSet::setCount(qint32 count) {
    m_count = count;
}

qint32 StickerSet::count() const {
    return m_count;
}

void StickerSet::setFlags(qint32 flags) {
    m_flags = flags;
}

qint32 StickerSet::flags() const {
    return m_flags;
}

void StickerSet::setHash(qint32 hash) {
    m_hash = hash;
}

qint32 StickerSet::hash() const {
    return m_hash;
}

void StickerSet::setId(qint64 id) {
    m_id = id;
}

qint64 StickerSet::id() const {
    return m_id;
}

void StickerSet::setInstalled(bool installed) {
    if(installed) m_flags = (m_flags | (1<<0));
    else m_flags = (m_flags & ~(1<<0));
}

bool StickerSet::installed() const {
    return (m_flags & 1<<0);
}

void StickerSet::setMasks(bool masks) {
    if(masks) m_flags = (m_flags | (1<<3));
    else m_flags = (m_flags & ~(1<<3));
}

bool StickerSet::masks() const {
    return (m_flags & 1<<3);
}

void StickerSet::setOfficial(bool official) {
    if(official) m_flags = (m_flags | (1<<2));
    else m_flags = (m_flags & ~(1<<2));
}

bool StickerSet::official() const {
    return (m_flags & 1<<2);
}

void StickerSet::setShortName(const QString &shortName) {
    m_shortName = shortName;
}

QString StickerSet::shortName() const {
    return m_shortName;
}

void StickerSet::setTitle(const QString &title) {
    m_title = title;
}

QString StickerSet::title() const {
    return m_title;
}

bool StickerSet::operator ==(const StickerSet &b) const {
    return m_classType == b.m_classType &&
           m_accessHash == b.m_accessHash &&
           m_count == b.m_count &&
           m_flags == b.m_flags &&
           m_hash == b.m_hash &&
           m_id == b.m_id &&
           m_shortName == b.m_shortName &&
           m_title == b.m_title;
}

StickerSet &StickerSet::operator =(const StickerSet &b) {
    m_classType = b.m_classType;
    m_accessHash = b.m_accessHash;
    m_count = b.m_count;
    m_flags = b.m_flags;
    m_hash = b.m_hash;
    m_id = b.m_id;
    m_shortName = b.m_shortName;
    m_title = b.m_title;
    setNull(b.isNull());
    return *this;
}

void StickerSet::setClassType(StickerSet::StickerSetClassType classType) {
    m_classType = classType;
}

StickerSet::StickerSetClassType StickerSet::classType() const {
    return m_classType;
}

bool StickerSet::fetch(InboundPkt *in) {
    LQTG_FETCH_LOG;
    int x = in->fetchInt();
    switch(x) {
    case typeStickerSet: {
        m_flags = in->fetchInt();
        m_id = in->fetchLong();
        m_accessHash = in->fetchLong();
        m_title = in->fetchQString();
        m_shortName = in->fetchQString();
        m_count = in->fetchInt();
        m_hash = in->fetchInt();
        m_classType = static_cast<StickerSetClassType>(x);
        return true;
    }
        break;

    default:
        LQTG_FETCH_ASSERT;
        return false;
    }
}

bool StickerSet::push(OutboundPkt *out) const {
    out->appendInt(m_classType);
    switch(m_classType) {
    case typeStickerSet: {
        out->appendInt(m_flags);
        out->appendLong(m_id);
        out->appendLong(m_accessHash);
        out->appendQString(m_title);
        out->appendQString(m_shortName);
        out->appendInt(m_count);
        out->appendInt(m_hash);
        return true;
    }
        break;

    default:
        return false;
    }
}

QMap<QString, QVariant> StickerSet::toMap() const {
    QMap<QString, QVariant> result;
    switch(static_cast<int>(m_classType)) {
    case typeStickerSet: {
        result["classType"] = "StickerSet::typeStickerSet";
        if( installed() ) result["installed"] = QString::number(installed());
        if( archived() ) result["archived"] = QString::number(archived());
        if( official() ) result["official"] = QString::number(official());
        if( masks() ) result["masks"] = QString::number(masks());
        if( id() ) result["id"] = QString::number(id());
        if( accessHash() ) result["accessHash"] = QString::number(accessHash());
        if( !m_title.isEmpty() ) result["title"] = QVariant::fromValue<QString>(m_title);
        if( !m_shortName.isEmpty() ) result["shortName"] = QVariant::fromValue<QString>(m_shortName);
        if( count() ) result["count"] = QString::number(count());
        if( hash() ) result["hash"] = QString::number(hash());
        return result;
    }
        break;

    default:
        return result;
    }
}

StickerSet StickerSet::fromMap(const QMap<QString, QVariant> &map) {
    StickerSet result;
    if(map.value("classType").toString() == "StickerSet::typeStickerSet") {
        result.setClassType(typeStickerSet);
        QVariant _installed_var = map.value("installed");
        if( !_installed_var.isNull() ) {
            _installed_var.convert( QVariant::nameToType("bool") );
            result.setInstalled( _installed_var.value<bool>() );
        }

        QVariant _archived_var = map.value("archived");
        if( !_archived_var.isNull() ) {
            _archived_var.convert( QVariant::nameToType("bool") );
            result.setArchived( _archived_var.value<bool>() );
        }

        QVariant _official_var = map.value("official");
        if( !_official_var.isNull() ) {
            _official_var.convert( QVariant::nameToType("bool") );
            result.setOfficial( _official_var.value<bool>() );
        }

        QVariant _masks_var = map.value("masks");
        if( !_masks_var.isNull() ) {
            _masks_var.convert( QVariant::nameToType("bool") );
            result.setMasks( _masks_var.value<bool>() );
        }

        QVariant _id_var = map.value("id");
        if( !_id_var.isNull() ) {
            _id_var.convert( QVariant::nameToType("qint64") );
            result.setId( _id_var.value<qint64>() );
        }

        QVariant _accessHash_var = map.value("accessHash");
        if( !_accessHash_var.isNull() ) {
            _accessHash_var.convert( QVariant::nameToType("qint64") );
            result.setAccessHash( _accessHash_var.value<qint64>() );
        }

        QVariant _title_var = map.value("title");
        if( !_title_var.isNull() ) {
            _title_var.convert( QVariant::nameToType("QString") );
            result.setTitle( _title_var.value<QString>() );
        }

        QVariant _shortName_var = map.value("shortName");
        if( !_shortName_var.isNull() ) {
            _shortName_var.convert( QVariant::nameToType("QString") );
            result.setShortName( _shortName_var.value<QString>() );
        }

        QVariant _count_var = map.value("count");
        if( !_count_var.isNull() ) {
            _count_var.convert( QVariant::nameToType("qint32") );
            result.setCount( _count_var.value<qint32>() );
        }

        QVariant _hash_var = map.value("hash");
        if( !_hash_var.isNull() ) {
            _hash_var.convert( QVariant::nameToType("qint32") );
            result.setHash( _hash_var.value<qint32>() );
        }

        return result;
    }
    return result;
}

StickerSet StickerSet::fromJson(const QString &json) {
    return StickerSet::fromMap(QJsonDocument::fromJson(json.toUtf8()).toVariant().toMap());
}

QByteArray StickerSet::getHash(QCryptographicHash::Algorithm alg) const {
    QByteArray data;
    QDataStream str(&data, QIODevice::WriteOnly);
    str << *this;
    return QCryptographicHash::hash(data, alg);
}

QDataStream &operator<<(QDataStream &stream, const StickerSet &item) {
    stream << static_cast<uint>(item.classType());
    switch(item.classType()) {
    case StickerSet::typeStickerSet:
        stream << item.flags();
        stream << item.id();
        stream << item.accessHash();
        stream << item.title();
        stream << item.shortName();
        stream << item.count();
        stream << item.hash();
        break;
    }
    return stream;
}

QDataStream &operator>>(QDataStream &stream, StickerSet &item) {
    uint type = 0;
    stream >> type;
    item.setClassType(static_cast<StickerSet::StickerSetClassType>(type));
    switch(type) {
    case StickerSet::typeStickerSet: {
        qint32 m_flags;
        stream >> m_flags;
        item.setFlags(m_flags);
        qint64 m_id;
        stream >> m_id;
        item.setId(m_id);
        qint64 m_access_hash;
        stream >> m_access_hash;
        item.setAccessHash(m_access_hash);
        QString m_title;
        stream >> m_title;
        item.setTitle(m_title);
        QString m_short_name;
        stream >> m_short_name;
        item.setShortName(m_short_name);
        qint32 m_count;
        stream >> m_count;
        item.setCount(m_count);
        qint32 m_hash;
        stream >> m_hash;
        item.setHash(m_hash);
    }
        break;
    }
    return stream;
}

/*QDebug operator<<(QDebug debug,  const StickerSet &item) {
    QDebugStateSaver saver(debug);
    Q_UNUSED(saver)
    debug.nospace() << "Telegram.StickerSet(";
    switch(item.classType()) {
    case StickerSet::typeStickerSet:
        debug.nospace() << "classType: typeStickerSet";
        debug.nospace() << ", flags: " << item.flags();
        debug.nospace() << ", id: " << item.id();
        debug.nospace() << ", accessHash: " << item.accessHash();
        debug.nospace() << ", title: " << item.title();
        debug.nospace() << ", shortName: " << item.shortName();
        debug.nospace() << ", count: " << item.count();
        debug.nospace() << ", hash: " << item.hash();
        break;
    }
    debug.nospace() << ")";
    return debug;
}
*/
