// This file generated by libqtelegram-code-generator.
// You can download it from: https://github.com/Aseman-Land/libqtelegram-code-generator
// DO NOT EDIT THIS FILE BY HAND -- YOUR CHANGES WILL BE OVERWRITTEN

#include "decryptedmessageaction.h"
#include "core/inboundpkt.h"
#include "core/outboundpkt.h"
#include "../coretypes.h"

#include <QDataStream>

DecryptedMessageAction::DecryptedMessageAction(DecryptedMessageActionClassType classType, InboundPkt *in) :
    m_endSeqNo(0),
    m_exchangeId(0),
    m_keyFingerprint(0),
    m_layer(0),
    m_startSeqNo(0),
    m_ttlSeconds(0),
    m_classType(classType)
{
    if(in) fetch(in);
}

DecryptedMessageAction::DecryptedMessageAction(InboundPkt *in) :
    m_endSeqNo(0),
    m_exchangeId(0),
    m_keyFingerprint(0),
    m_layer(0),
    m_startSeqNo(0),
    m_ttlSeconds(0),
    m_classType(typeDecryptedMessageActionSetMessageTTLSecret8)
{
    fetch(in);
}

DecryptedMessageAction::DecryptedMessageAction(const DecryptedMessageAction &another) :
    TelegramTypeObject(),
    m_endSeqNo(0),
    m_exchangeId(0),
    m_keyFingerprint(0),
    m_layer(0),
    m_startSeqNo(0),
    m_ttlSeconds(0),
    m_classType(typeDecryptedMessageActionSetMessageTTLSecret8)
{
    operator=(another);
}

DecryptedMessageAction::DecryptedMessageAction(const Null &null) :
    TelegramTypeObject(null),
    m_endSeqNo(0),
    m_exchangeId(0),
    m_keyFingerprint(0),
    m_layer(0),
    m_startSeqNo(0),
    m_ttlSeconds(0),
    m_classType(typeDecryptedMessageActionSetMessageTTLSecret8)
{
}

DecryptedMessageAction::~DecryptedMessageAction() {

}

void DecryptedMessageAction::setAction(const SendMessageAction &action) {
    m_action = action;
}

SendMessageAction DecryptedMessageAction::action() const {
    return m_action;
}

void DecryptedMessageAction::setEndSeqNo(qint32 endSeqNo) {
    m_endSeqNo = endSeqNo;
}

qint32 DecryptedMessageAction::endSeqNo() const {
    return m_endSeqNo;
}

void DecryptedMessageAction::setExchangeId(qint64 exchangeId) {
    m_exchangeId = exchangeId;
}

qint64 DecryptedMessageAction::exchangeId() const {
    return m_exchangeId;
}

void DecryptedMessageAction::setGA(const QByteArray &gA) {
    m_gA = gA;
}

QByteArray DecryptedMessageAction::gA() const {
    return m_gA;
}

void DecryptedMessageAction::setGB(const QByteArray &gB) {
    m_gB = gB;
}

QByteArray DecryptedMessageAction::gB() const {
    return m_gB;
}

void DecryptedMessageAction::setKeyFingerprint(qint64 keyFingerprint) {
    m_keyFingerprint = keyFingerprint;
}

qint64 DecryptedMessageAction::keyFingerprint() const {
    return m_keyFingerprint;
}

void DecryptedMessageAction::setLayer(qint32 layer) {
    m_layer = layer;
}

qint32 DecryptedMessageAction::layer() const {
    return m_layer;
}

void DecryptedMessageAction::setRandomIds(const QList<qint64> &randomIds) {
    m_randomIds = randomIds;
}

QList<qint64> DecryptedMessageAction::randomIds() const {
    return m_randomIds;
}

void DecryptedMessageAction::setStartSeqNo(qint32 startSeqNo) {
    m_startSeqNo = startSeqNo;
}

qint32 DecryptedMessageAction::startSeqNo() const {
    return m_startSeqNo;
}

void DecryptedMessageAction::setTtlSeconds(qint32 ttlSeconds) {
    m_ttlSeconds = ttlSeconds;
}

qint32 DecryptedMessageAction::ttlSeconds() const {
    return m_ttlSeconds;
}

bool DecryptedMessageAction::operator ==(const DecryptedMessageAction &b) const {
    return m_classType == b.m_classType &&
           m_action == b.m_action &&
           m_endSeqNo == b.m_endSeqNo &&
           m_exchangeId == b.m_exchangeId &&
           m_gA == b.m_gA &&
           m_gB == b.m_gB &&
           m_keyFingerprint == b.m_keyFingerprint &&
           m_layer == b.m_layer &&
           m_randomIds == b.m_randomIds &&
           m_startSeqNo == b.m_startSeqNo &&
           m_ttlSeconds == b.m_ttlSeconds;
}

DecryptedMessageAction &DecryptedMessageAction::operator =(const DecryptedMessageAction &b) {
    m_classType = b.m_classType;
    m_action = b.m_action;
    m_endSeqNo = b.m_endSeqNo;
    m_exchangeId = b.m_exchangeId;
    m_gA = b.m_gA;
    m_gB = b.m_gB;
    m_keyFingerprint = b.m_keyFingerprint;
    m_layer = b.m_layer;
    m_randomIds = b.m_randomIds;
    m_startSeqNo = b.m_startSeqNo;
    m_ttlSeconds = b.m_ttlSeconds;
    setNull(b.isNull());
    return *this;
}

void DecryptedMessageAction::setClassType(DecryptedMessageAction::DecryptedMessageActionClassType classType) {
    m_classType = classType;
}

DecryptedMessageAction::DecryptedMessageActionClassType DecryptedMessageAction::classType() const {
    return m_classType;
}

bool DecryptedMessageAction::fetch(InboundPkt *in) {
    LQTG_FETCH_LOG;
    int x = in->fetchInt();
    switch(x) {
    case typeDecryptedMessageActionSetMessageTTLSecret8: {
        m_ttlSeconds = in->fetchInt();
        m_classType = static_cast<DecryptedMessageActionClassType>(x);
        return true;
    }
        break;

    case typeDecryptedMessageActionReadMessagesSecret8: {
        if(in->fetchInt() != (qint32)CoreTypes::typeVector) return false;
        qint32 m_randomIds_length = in->fetchInt();
        m_randomIds.clear();
        for (qint32 i = 0; i < m_randomIds_length; i++) {
            qint64 type;
            type = in->fetchLong();
            m_randomIds.append(type);
        }
        m_classType = static_cast<DecryptedMessageActionClassType>(x);
        return true;
    }
        break;

    case typeDecryptedMessageActionDeleteMessagesSecret8: {
        if(in->fetchInt() != (qint32)CoreTypes::typeVector) return false;
        qint32 m_randomIds_length = in->fetchInt();
        m_randomIds.clear();
        for (qint32 i = 0; i < m_randomIds_length; i++) {
            qint64 type;
            type = in->fetchLong();
            m_randomIds.append(type);
        }
        m_classType = static_cast<DecryptedMessageActionClassType>(x);
        return true;
    }
        break;

    case typeDecryptedMessageActionScreenshotMessagesSecret8: {
        if(in->fetchInt() != (qint32)CoreTypes::typeVector) return false;
        qint32 m_randomIds_length = in->fetchInt();
        m_randomIds.clear();
        for (qint32 i = 0; i < m_randomIds_length; i++) {
            qint64 type;
            type = in->fetchLong();
            m_randomIds.append(type);
        }
        m_classType = static_cast<DecryptedMessageActionClassType>(x);
        return true;
    }
        break;

    case typeDecryptedMessageActionFlushHistorySecret8: {
        m_classType = static_cast<DecryptedMessageActionClassType>(x);
        return true;
    }
        break;

    case typeDecryptedMessageActionResendSecret17: {
        m_startSeqNo = in->fetchInt();
        m_endSeqNo = in->fetchInt();
        m_classType = static_cast<DecryptedMessageActionClassType>(x);
        return true;
    }
        break;

    case typeDecryptedMessageActionNotifyLayerSecret17: {
        m_layer = in->fetchInt();
        m_classType = static_cast<DecryptedMessageActionClassType>(x);
        return true;
    }
        break;

    case typeDecryptedMessageActionTypingSecret17: {
        m_action.fetch(in);
        m_classType = static_cast<DecryptedMessageActionClassType>(x);
        return true;
    }
        break;

    case typeDecryptedMessageActionRequestKeySecret20: {
        m_exchangeId = in->fetchLong();
        m_gA = in->fetchBytes();
        m_classType = static_cast<DecryptedMessageActionClassType>(x);
        return true;
    }
        break;

    case typeDecryptedMessageActionAcceptKeySecret20: {
        m_exchangeId = in->fetchLong();
        m_gB = in->fetchBytes();
        m_keyFingerprint = in->fetchLong();
        m_classType = static_cast<DecryptedMessageActionClassType>(x);
        return true;
    }
        break;

    case typeDecryptedMessageActionAbortKeySecret20: {
        m_exchangeId = in->fetchLong();
        m_classType = static_cast<DecryptedMessageActionClassType>(x);
        return true;
    }
        break;

    case typeDecryptedMessageActionCommitKeySecret20: {
        m_exchangeId = in->fetchLong();
        m_keyFingerprint = in->fetchLong();
        m_classType = static_cast<DecryptedMessageActionClassType>(x);
        return true;
    }
        break;

    case typeDecryptedMessageActionNoopSecret20: {
        m_classType = static_cast<DecryptedMessageActionClassType>(x);
        return true;
    }
        break;

    default:
        LQTG_FETCH_ASSERT;
        return false;
    }
}

bool DecryptedMessageAction::push(OutboundPkt *out) const {
    out->appendInt(m_classType);
    switch(m_classType) {
    case typeDecryptedMessageActionSetMessageTTLSecret8: {
        out->appendInt(m_ttlSeconds);
        return true;
    }
        break;

    case typeDecryptedMessageActionReadMessagesSecret8: {
        out->appendInt(CoreTypes::typeVector);
        out->appendInt(m_randomIds.count());
        for (qint32 i = 0; i < m_randomIds.count(); i++) {
            out->appendLong(m_randomIds[i]);
        }
        return true;
    }
        break;

    case typeDecryptedMessageActionDeleteMessagesSecret8: {
        out->appendInt(CoreTypes::typeVector);
        out->appendInt(m_randomIds.count());
        for (qint32 i = 0; i < m_randomIds.count(); i++) {
            out->appendLong(m_randomIds[i]);
        }
        return true;
    }
        break;

    case typeDecryptedMessageActionScreenshotMessagesSecret8: {
        out->appendInt(CoreTypes::typeVector);
        out->appendInt(m_randomIds.count());
        for (qint32 i = 0; i < m_randomIds.count(); i++) {
            out->appendLong(m_randomIds[i]);
        }
        return true;
    }
        break;

    case typeDecryptedMessageActionFlushHistorySecret8: {
        return true;
    }
        break;

    case typeDecryptedMessageActionResendSecret17: {
        out->appendInt(m_startSeqNo);
        out->appendInt(m_endSeqNo);
        return true;
    }
        break;

    case typeDecryptedMessageActionNotifyLayerSecret17: {
        out->appendInt(m_layer);
        return true;
    }
        break;

    case typeDecryptedMessageActionTypingSecret17: {
        m_action.push(out);
        return true;
    }
        break;

    case typeDecryptedMessageActionRequestKeySecret20: {
        out->appendLong(m_exchangeId);
        out->appendBytes(m_gA);
        return true;
    }
        break;

    case typeDecryptedMessageActionAcceptKeySecret20: {
        out->appendLong(m_exchangeId);
        out->appendBytes(m_gB);
        out->appendLong(m_keyFingerprint);
        return true;
    }
        break;

    case typeDecryptedMessageActionAbortKeySecret20: {
        out->appendLong(m_exchangeId);
        return true;
    }
        break;

    case typeDecryptedMessageActionCommitKeySecret20: {
        out->appendLong(m_exchangeId);
        out->appendLong(m_keyFingerprint);
        return true;
    }
        break;

    case typeDecryptedMessageActionNoopSecret20: {
        return true;
    }
        break;

    default:
        return false;
    }
}

QMap<QString, QVariant> DecryptedMessageAction::toMap() const {
    QMap<QString, QVariant> result;
    switch(static_cast<int>(m_classType)) {
    case typeDecryptedMessageActionSetMessageTTLSecret8: {
        result["classType"] = "DecryptedMessageAction::typeDecryptedMessageActionSetMessageTTLSecret8";
        if( ttlSeconds() ) result["ttlSeconds"] = QString::number(ttlSeconds());
        return result;
    }
        break;

    case typeDecryptedMessageActionReadMessagesSecret8: {
        result["classType"] = "DecryptedMessageAction::typeDecryptedMessageActionReadMessagesSecret8";
        QList<QVariant> _randomIds;
        for(const qint64 &m__type: m_randomIds)
            _randomIds << QVariant::fromValue<qint64>(m__type);
        result["randomIds"] = _randomIds;
        return result;
    }
        break;

    case typeDecryptedMessageActionDeleteMessagesSecret8: {
        result["classType"] = "DecryptedMessageAction::typeDecryptedMessageActionDeleteMessagesSecret8";
        QList<QVariant> _randomIds;
        for(const qint64 &m__type: m_randomIds)
            _randomIds << QVariant::fromValue<qint64>(m__type);
        result["randomIds"] = _randomIds;
        return result;
    }
        break;

    case typeDecryptedMessageActionScreenshotMessagesSecret8: {
        result["classType"] = "DecryptedMessageAction::typeDecryptedMessageActionScreenshotMessagesSecret8";
        QList<QVariant> _randomIds;
        for(const qint64 &m__type: m_randomIds)
            _randomIds << QVariant::fromValue<qint64>(m__type);
        result["randomIds"] = _randomIds;
        return result;
    }
        break;

    case typeDecryptedMessageActionFlushHistorySecret8: {
        result["classType"] = "DecryptedMessageAction::typeDecryptedMessageActionFlushHistorySecret8";
        return result;
    }
        break;

    case typeDecryptedMessageActionResendSecret17: {
        result["classType"] = "DecryptedMessageAction::typeDecryptedMessageActionResendSecret17";
        if( startSeqNo() ) result["startSeqNo"] = QString::number(startSeqNo());
        if( endSeqNo() ) result["endSeqNo"] = QString::number(endSeqNo());
        return result;
    }
        break;

    case typeDecryptedMessageActionNotifyLayerSecret17: {
        result["classType"] = "DecryptedMessageAction::typeDecryptedMessageActionNotifyLayerSecret17";
        if( layer() ) result["layer"] = QString::number(layer());
        return result;
    }
        break;

    case typeDecryptedMessageActionTypingSecret17: {
        result["classType"] = "DecryptedMessageAction::typeDecryptedMessageActionTypingSecret17";
        if( !m_action.isNull() ) result["action"] = m_action.toMap();
        return result;
    }
        break;

    case typeDecryptedMessageActionRequestKeySecret20: {
        result["classType"] = "DecryptedMessageAction::typeDecryptedMessageActionRequestKeySecret20";
        if( exchangeId() ) result["exchangeId"] = QString::number(exchangeId());
        if( !m_gA.isEmpty() ) result["gA"] = QVariant::fromValue<QByteArray>(m_gA);
        return result;
    }
        break;

    case typeDecryptedMessageActionAcceptKeySecret20: {
        result["classType"] = "DecryptedMessageAction::typeDecryptedMessageActionAcceptKeySecret20";
        if( exchangeId() ) result["exchangeId"] = QString::number(exchangeId());
        if( !m_gB.isEmpty() ) result["gB"] = QVariant::fromValue<QByteArray>(m_gB);
        if( keyFingerprint() ) result["keyFingerprint"] = QString::number(keyFingerprint());
        return result;
    }
        break;

    case typeDecryptedMessageActionAbortKeySecret20: {
        result["classType"] = "DecryptedMessageAction::typeDecryptedMessageActionAbortKeySecret20";
        if( exchangeId() ) result["exchangeId"] = QString::number(exchangeId());
        return result;
    }
        break;

    case typeDecryptedMessageActionCommitKeySecret20: {
        result["classType"] = "DecryptedMessageAction::typeDecryptedMessageActionCommitKeySecret20";
        if( exchangeId() ) result["exchangeId"] = QString::number(exchangeId());
        if( keyFingerprint() ) result["keyFingerprint"] = QString::number(keyFingerprint());
        return result;
    }
        break;

    case typeDecryptedMessageActionNoopSecret20: {
        result["classType"] = "DecryptedMessageAction::typeDecryptedMessageActionNoopSecret20";
        return result;
    }
        break;

    default:
        return result;
    }
}

DecryptedMessageAction DecryptedMessageAction::fromMap(const QMap<QString, QVariant> &map) {
    DecryptedMessageAction result;
    if(map.value("classType").toString() == "DecryptedMessageAction::typeDecryptedMessageActionSetMessageTTLSecret8") {
        result.setClassType(typeDecryptedMessageActionSetMessageTTLSecret8);
        QVariant _ttlSeconds_var = map.value("ttlSeconds");
        if( !_ttlSeconds_var.isNull() ) {
            _ttlSeconds_var.convert( QVariant::nameToType("qint32") );
            result.setTtlSeconds( _ttlSeconds_var.value<qint32>() );
        }

        return result;
    }
    if(map.value("classType").toString() == "DecryptedMessageAction::typeDecryptedMessageActionReadMessagesSecret8") {
        result.setClassType(typeDecryptedMessageActionReadMessagesSecret8);
        QList<QVariant> map_randomIds = map["randomIds"].toList();
        QList<qint64> _randomIds;
        for(const QVariant &var: map_randomIds)
            _randomIds << var.value<qint64>();;
        result.setRandomIds(_randomIds);
        return result;
    }
    if(map.value("classType").toString() == "DecryptedMessageAction::typeDecryptedMessageActionDeleteMessagesSecret8") {
        result.setClassType(typeDecryptedMessageActionDeleteMessagesSecret8);
        QList<QVariant> map_randomIds = map["randomIds"].toList();
        QList<qint64> _randomIds;
        for(const QVariant &var: map_randomIds)
            _randomIds << var.value<qint64>();;
        result.setRandomIds(_randomIds);
        return result;
    }
    if(map.value("classType").toString() == "DecryptedMessageAction::typeDecryptedMessageActionScreenshotMessagesSecret8") {
        result.setClassType(typeDecryptedMessageActionScreenshotMessagesSecret8);
        QList<QVariant> map_randomIds = map["randomIds"].toList();
        QList<qint64> _randomIds;
        for(const QVariant &var: map_randomIds)
            _randomIds << var.value<qint64>();;
        result.setRandomIds(_randomIds);
        return result;
    }
    if(map.value("classType").toString() == "DecryptedMessageAction::typeDecryptedMessageActionFlushHistorySecret8") {
        result.setClassType(typeDecryptedMessageActionFlushHistorySecret8);
        return result;
    }
    if(map.value("classType").toString() == "DecryptedMessageAction::typeDecryptedMessageActionResendSecret17") {
        result.setClassType(typeDecryptedMessageActionResendSecret17);
        QVariant _startSeqNo_var = map.value("startSeqNo");
        if( !_startSeqNo_var.isNull() ) {
            _startSeqNo_var.convert( QVariant::nameToType("qint32") );
            result.setStartSeqNo( _startSeqNo_var.value<qint32>() );
        }

        QVariant _endSeqNo_var = map.value("endSeqNo");
        if( !_endSeqNo_var.isNull() ) {
            _endSeqNo_var.convert( QVariant::nameToType("qint32") );
            result.setEndSeqNo( _endSeqNo_var.value<qint32>() );
        }

        return result;
    }
    if(map.value("classType").toString() == "DecryptedMessageAction::typeDecryptedMessageActionNotifyLayerSecret17") {
        result.setClassType(typeDecryptedMessageActionNotifyLayerSecret17);
        QVariant _layer_var = map.value("layer");
        if( !_layer_var.isNull() ) {
            _layer_var.convert( QVariant::nameToType("qint32") );
            result.setLayer( _layer_var.value<qint32>() );
        }

        return result;
    }
    if(map.value("classType").toString() == "DecryptedMessageAction::typeDecryptedMessageActionTypingSecret17") {
        result.setClassType(typeDecryptedMessageActionTypingSecret17);
        QVariant _action_var = map.value("action");
        if( !_action_var.isNull() )
            result.setAction( SendMessageAction::fromMap(_action_var.toMap()) );

        return result;
    }
    if(map.value("classType").toString() == "DecryptedMessageAction::typeDecryptedMessageActionRequestKeySecret20") {
        result.setClassType(typeDecryptedMessageActionRequestKeySecret20);
        QVariant _exchangeId_var = map.value("exchangeId");
        if( !_exchangeId_var.isNull() ) {
            _exchangeId_var.convert( QVariant::nameToType("qint64") );
            result.setExchangeId( _exchangeId_var.value<qint64>() );
        }

        QVariant _gA_var = map.value("gA");
        if( !_gA_var.isNull() ) {
            _gA_var.convert( QVariant::nameToType("QByteArray") );
            result.setGA( _gA_var.value<QByteArray>() );
        }

        return result;
    }
    if(map.value("classType").toString() == "DecryptedMessageAction::typeDecryptedMessageActionAcceptKeySecret20") {
        result.setClassType(typeDecryptedMessageActionAcceptKeySecret20);
        QVariant _exchangeId_var = map.value("exchangeId");
        if( !_exchangeId_var.isNull() ) {
            _exchangeId_var.convert( QVariant::nameToType("qint64") );
            result.setExchangeId( _exchangeId_var.value<qint64>() );
        }

        QVariant _gB_var = map.value("gB");
        if( !_gB_var.isNull() ) {
            _gB_var.convert( QVariant::nameToType("QByteArray") );
            result.setGB( _gB_var.value<QByteArray>() );
        }

        QVariant _keyFingerprint_var = map.value("keyFingerprint");
        if( !_keyFingerprint_var.isNull() ) {
            _keyFingerprint_var.convert( QVariant::nameToType("qint64") );
            result.setKeyFingerprint( _keyFingerprint_var.value<qint64>() );
        }

        return result;
    }
    if(map.value("classType").toString() == "DecryptedMessageAction::typeDecryptedMessageActionAbortKeySecret20") {
        result.setClassType(typeDecryptedMessageActionAbortKeySecret20);
        QVariant _exchangeId_var = map.value("exchangeId");
        if( !_exchangeId_var.isNull() ) {
            _exchangeId_var.convert( QVariant::nameToType("qint64") );
            result.setExchangeId( _exchangeId_var.value<qint64>() );
        }

        return result;
    }
    if(map.value("classType").toString() == "DecryptedMessageAction::typeDecryptedMessageActionCommitKeySecret20") {
        result.setClassType(typeDecryptedMessageActionCommitKeySecret20);
        QVariant _exchangeId_var = map.value("exchangeId");
        if( !_exchangeId_var.isNull() ) {
            _exchangeId_var.convert( QVariant::nameToType("qint64") );
            result.setExchangeId( _exchangeId_var.value<qint64>() );
        }

        QVariant _keyFingerprint_var = map.value("keyFingerprint");
        if( !_keyFingerprint_var.isNull() ) {
            _keyFingerprint_var.convert( QVariant::nameToType("qint64") );
            result.setKeyFingerprint( _keyFingerprint_var.value<qint64>() );
        }

        return result;
    }
    if(map.value("classType").toString() == "DecryptedMessageAction::typeDecryptedMessageActionNoopSecret20") {
        result.setClassType(typeDecryptedMessageActionNoopSecret20);
        return result;
    }
    return result;
}

DecryptedMessageAction DecryptedMessageAction::fromJson(const QString &json) {
    return DecryptedMessageAction::fromMap(QJsonDocument::fromJson(json.toUtf8()).toVariant().toMap());
}

QByteArray DecryptedMessageAction::getHash(QCryptographicHash::Algorithm alg) const {
    QByteArray data;
    QDataStream str(&data, QIODevice::WriteOnly);
    str << *this;
    return QCryptographicHash::hash(data, alg);
}

QDataStream &operator<<(QDataStream &stream, const DecryptedMessageAction &item) {
    stream << static_cast<uint>(item.classType());
    switch(item.classType()) {
    case DecryptedMessageAction::typeDecryptedMessageActionSetMessageTTLSecret8:
        stream << item.ttlSeconds();
        break;
    case DecryptedMessageAction::typeDecryptedMessageActionReadMessagesSecret8:
        stream << item.randomIds();
        break;
    case DecryptedMessageAction::typeDecryptedMessageActionDeleteMessagesSecret8:
        stream << item.randomIds();
        break;
    case DecryptedMessageAction::typeDecryptedMessageActionScreenshotMessagesSecret8:
        stream << item.randomIds();
        break;
    case DecryptedMessageAction::typeDecryptedMessageActionFlushHistorySecret8:

        break;
    case DecryptedMessageAction::typeDecryptedMessageActionResendSecret17:
        stream << item.startSeqNo();
        stream << item.endSeqNo();
        break;
    case DecryptedMessageAction::typeDecryptedMessageActionNotifyLayerSecret17:
        stream << item.layer();
        break;
    case DecryptedMessageAction::typeDecryptedMessageActionTypingSecret17:
        stream << item.action();
        break;
    case DecryptedMessageAction::typeDecryptedMessageActionRequestKeySecret20:
        stream << item.exchangeId();
        stream << item.gA();
        break;
    case DecryptedMessageAction::typeDecryptedMessageActionAcceptKeySecret20:
        stream << item.exchangeId();
        stream << item.gB();
        stream << item.keyFingerprint();
        break;
    case DecryptedMessageAction::typeDecryptedMessageActionAbortKeySecret20:
        stream << item.exchangeId();
        break;
    case DecryptedMessageAction::typeDecryptedMessageActionCommitKeySecret20:
        stream << item.exchangeId();
        stream << item.keyFingerprint();
        break;
    case DecryptedMessageAction::typeDecryptedMessageActionNoopSecret20:

        break;
    }
    return stream;
}

QDataStream &operator>>(QDataStream &stream, DecryptedMessageAction &item) {
    uint type = 0;
    stream >> type;
    item.setClassType(static_cast<DecryptedMessageAction::DecryptedMessageActionClassType>(type));
    switch(type) {
    case DecryptedMessageAction::typeDecryptedMessageActionSetMessageTTLSecret8: {
        qint32 m_ttl_seconds;
        stream >> m_ttl_seconds;
        item.setTtlSeconds(m_ttl_seconds);
    }
        break;
    case DecryptedMessageAction::typeDecryptedMessageActionReadMessagesSecret8: {
        QList<qint64> m_random_ids;
        stream >> m_random_ids;
        item.setRandomIds(m_random_ids);
    }
        break;
    case DecryptedMessageAction::typeDecryptedMessageActionDeleteMessagesSecret8: {
        QList<qint64> m_random_ids;
        stream >> m_random_ids;
        item.setRandomIds(m_random_ids);
    }
        break;
    case DecryptedMessageAction::typeDecryptedMessageActionScreenshotMessagesSecret8: {
        QList<qint64> m_random_ids;
        stream >> m_random_ids;
        item.setRandomIds(m_random_ids);
    }
        break;
    case DecryptedMessageAction::typeDecryptedMessageActionFlushHistorySecret8: {

    }
        break;
    case DecryptedMessageAction::typeDecryptedMessageActionResendSecret17: {
        qint32 m_start_seq_no;
        stream >> m_start_seq_no;
        item.setStartSeqNo(m_start_seq_no);
        qint32 m_end_seq_no;
        stream >> m_end_seq_no;
        item.setEndSeqNo(m_end_seq_no);
    }
        break;
    case DecryptedMessageAction::typeDecryptedMessageActionNotifyLayerSecret17: {
        qint32 m_layer;
        stream >> m_layer;
        item.setLayer(m_layer);
    }
        break;
    case DecryptedMessageAction::typeDecryptedMessageActionTypingSecret17: {
        SendMessageAction m_action;
        stream >> m_action;
        item.setAction(m_action);
    }
        break;
    case DecryptedMessageAction::typeDecryptedMessageActionRequestKeySecret20: {
        qint64 m_exchange_id;
        stream >> m_exchange_id;
        item.setExchangeId(m_exchange_id);
        QByteArray m_g_a;
        stream >> m_g_a;
        item.setGA(m_g_a);
    }
        break;
    case DecryptedMessageAction::typeDecryptedMessageActionAcceptKeySecret20: {
        qint64 m_exchange_id;
        stream >> m_exchange_id;
        item.setExchangeId(m_exchange_id);
        QByteArray m_g_b;
        stream >> m_g_b;
        item.setGB(m_g_b);
        qint64 m_key_fingerprint;
        stream >> m_key_fingerprint;
        item.setKeyFingerprint(m_key_fingerprint);
    }
        break;
    case DecryptedMessageAction::typeDecryptedMessageActionAbortKeySecret20: {
        qint64 m_exchange_id;
        stream >> m_exchange_id;
        item.setExchangeId(m_exchange_id);
    }
        break;
    case DecryptedMessageAction::typeDecryptedMessageActionCommitKeySecret20: {
        qint64 m_exchange_id;
        stream >> m_exchange_id;
        item.setExchangeId(m_exchange_id);
        qint64 m_key_fingerprint;
        stream >> m_key_fingerprint;
        item.setKeyFingerprint(m_key_fingerprint);
    }
        break;
    case DecryptedMessageAction::typeDecryptedMessageActionNoopSecret20: {

    }
        break;
    }
    return stream;
}

/*QDebug operator<<(QDebug debug,  const DecryptedMessageAction &item) {
    QDebugStateSaver saver(debug);
    Q_UNUSED(saver)
    debug.nospace() << "Telegram.DecryptedMessageAction(";
    switch(item.classType()) {
    case DecryptedMessageAction::typeDecryptedMessageActionSetMessageTTLSecret8:
        debug.nospace() << "classType: typeDecryptedMessageActionSetMessageTTLSecret8";
        debug.nospace() << ", ttlSeconds: " << item.ttlSeconds();
        break;
    case DecryptedMessageAction::typeDecryptedMessageActionReadMessagesSecret8:
        debug.nospace() << "classType: typeDecryptedMessageActionReadMessagesSecret8";
        debug.nospace() << ", randomIds: " << item.randomIds();
        break;
    case DecryptedMessageAction::typeDecryptedMessageActionDeleteMessagesSecret8:
        debug.nospace() << "classType: typeDecryptedMessageActionDeleteMessagesSecret8";
        debug.nospace() << ", randomIds: " << item.randomIds();
        break;
    case DecryptedMessageAction::typeDecryptedMessageActionScreenshotMessagesSecret8:
        debug.nospace() << "classType: typeDecryptedMessageActionScreenshotMessagesSecret8";
        debug.nospace() << ", randomIds: " << item.randomIds();
        break;
    case DecryptedMessageAction::typeDecryptedMessageActionFlushHistorySecret8:
        debug.nospace() << "classType: typeDecryptedMessageActionFlushHistorySecret8";
        break;
    case DecryptedMessageAction::typeDecryptedMessageActionResendSecret17:
        debug.nospace() << "classType: typeDecryptedMessageActionResendSecret17";
        debug.nospace() << ", startSeqNo: " << item.startSeqNo();
        debug.nospace() << ", endSeqNo: " << item.endSeqNo();
        break;
    case DecryptedMessageAction::typeDecryptedMessageActionNotifyLayerSecret17:
        debug.nospace() << "classType: typeDecryptedMessageActionNotifyLayerSecret17";
        debug.nospace() << ", layer: " << item.layer();
        break;
    case DecryptedMessageAction::typeDecryptedMessageActionTypingSecret17:
        debug.nospace() << "classType: typeDecryptedMessageActionTypingSecret17";
        debug.nospace() << ", action: " << item.action();
        break;
    case DecryptedMessageAction::typeDecryptedMessageActionRequestKeySecret20:
        debug.nospace() << "classType: typeDecryptedMessageActionRequestKeySecret20";
        debug.nospace() << ", exchangeId: " << item.exchangeId();
        debug.nospace() << ", gA: " << item.gA();
        break;
    case DecryptedMessageAction::typeDecryptedMessageActionAcceptKeySecret20:
        debug.nospace() << "classType: typeDecryptedMessageActionAcceptKeySecret20";
        debug.nospace() << ", exchangeId: " << item.exchangeId();
        debug.nospace() << ", gB: " << item.gB();
        debug.nospace() << ", keyFingerprint: " << item.keyFingerprint();
        break;
    case DecryptedMessageAction::typeDecryptedMessageActionAbortKeySecret20:
        debug.nospace() << "classType: typeDecryptedMessageActionAbortKeySecret20";
        debug.nospace() << ", exchangeId: " << item.exchangeId();
        break;
    case DecryptedMessageAction::typeDecryptedMessageActionCommitKeySecret20:
        debug.nospace() << "classType: typeDecryptedMessageActionCommitKeySecret20";
        debug.nospace() << ", exchangeId: " << item.exchangeId();
        debug.nospace() << ", keyFingerprint: " << item.keyFingerprint();
        break;
    case DecryptedMessageAction::typeDecryptedMessageActionNoopSecret20:
        debug.nospace() << "classType: typeDecryptedMessageActionNoopSecret20";
        break;
    }
    debug.nospace() << ")";
    return debug;
}
*/
