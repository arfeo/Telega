// This file generated by libqtelegram-code-generator.
// You can download it from: https://github.com/Aseman-Land/libqtelegram-code-generator
// DO NOT EDIT THIS FILE BY HAND -- YOUR CHANGES WILL BE OVERWRITTEN

#include "authcheckedphone.h"
#include "core/inboundpkt.h"
#include "core/outboundpkt.h"
#include "../coretypes.h"

#include <QDataStream>

AuthCheckedPhone::AuthCheckedPhone(AuthCheckedPhoneClassType classType, InboundPkt *in) :
    m_phoneRegistered(false),
    m_classType(classType)
{
    if(in) fetch(in);
}

AuthCheckedPhone::AuthCheckedPhone(InboundPkt *in) :
    m_phoneRegistered(false),
    m_classType(typeAuthCheckedPhone)
{
    fetch(in);
}

AuthCheckedPhone::AuthCheckedPhone(const AuthCheckedPhone &another) :
    TelegramTypeObject(),
    m_phoneRegistered(false),
    m_classType(typeAuthCheckedPhone)
{
    operator=(another);
}

AuthCheckedPhone::AuthCheckedPhone(const Null &null) :
    TelegramTypeObject(null),
    m_phoneRegistered(false),
    m_classType(typeAuthCheckedPhone)
{
}

AuthCheckedPhone::~AuthCheckedPhone() {
    
}

void AuthCheckedPhone::setPhoneRegistered(bool phoneRegistered) {
    m_phoneRegistered = phoneRegistered;
}

bool AuthCheckedPhone::phoneRegistered() const {
    return m_phoneRegistered;
}

bool AuthCheckedPhone::operator ==(const AuthCheckedPhone &b) const {
    return m_classType == b.m_classType &&
           m_phoneRegistered == b.m_phoneRegistered;
}

AuthCheckedPhone &AuthCheckedPhone::operator =(const AuthCheckedPhone &b) {
    m_classType = b.m_classType;
    m_phoneRegistered = b.m_phoneRegistered;
    setNull(b.isNull());
    return *this;
}

void AuthCheckedPhone::setClassType(AuthCheckedPhone::AuthCheckedPhoneClassType classType) {
    m_classType = classType;
}

AuthCheckedPhone::AuthCheckedPhoneClassType AuthCheckedPhone::classType() const {
    return m_classType;
}

bool AuthCheckedPhone::fetch(InboundPkt *in) {
    LQTG_FETCH_LOG;
    int x = in->fetchInt();
    switch(x) {
    case typeAuthCheckedPhone: {
        m_phoneRegistered = in->fetchBool();
        m_classType = static_cast<AuthCheckedPhoneClassType>(x);
        return true;
    }
        break;
    
    default:
        LQTG_FETCH_ASSERT;
        return false;
    }
}

bool AuthCheckedPhone::push(OutboundPkt *out) const {
    out->appendInt(m_classType);
    switch(m_classType) {
    case typeAuthCheckedPhone: {
        out->appendBool(m_phoneRegistered);
        return true;
    }
        break;
    
    default:
        return false;
    }
}

QMap<QString, QVariant> AuthCheckedPhone::toMap() const {
    QMap<QString, QVariant> result;
    switch(static_cast<int>(m_classType)) {
    case typeAuthCheckedPhone: {
        result["classType"] = "AuthCheckedPhone::typeAuthCheckedPhone";
        if( phoneRegistered() ) result["phoneRegistered"] = QString::number(phoneRegistered());
        return result;
    }
        break;
    
    default:
        return result;
    }
}

AuthCheckedPhone AuthCheckedPhone::fromMap(const QMap<QString, QVariant> &map) {
    AuthCheckedPhone result;
    if(map.value("classType").toString() == "AuthCheckedPhone::typeAuthCheckedPhone") {
        result.setClassType(typeAuthCheckedPhone);
        QVariant _phoneRegistered_var = map.value("phoneRegistered");
        if( !_phoneRegistered_var.isNull() ) {
            _phoneRegistered_var.convert( QVariant::nameToType("bool") );
            result.setPhoneRegistered( _phoneRegistered_var.value<bool>() );
        }
        
        return result;
    }
    return result;
}

AuthCheckedPhone AuthCheckedPhone::fromJson(const QString &json) {
    return AuthCheckedPhone::fromMap(QJsonDocument::fromJson(json.toUtf8()).toVariant().toMap());
}

QByteArray AuthCheckedPhone::getHash(QCryptographicHash::Algorithm alg) const {
    QByteArray data;
    QDataStream str(&data, QIODevice::WriteOnly);
    str << *this;
    return QCryptographicHash::hash(data, alg);
}

QDataStream &operator<<(QDataStream &stream, const AuthCheckedPhone &item) {
    stream << static_cast<uint>(item.classType());
    switch(item.classType()) {
    case AuthCheckedPhone::typeAuthCheckedPhone:
        stream << item.phoneRegistered();
        break;
    }
    return stream;
}

QDataStream &operator>>(QDataStream &stream, AuthCheckedPhone &item) {
    uint type = 0;
    stream >> type;
    item.setClassType(static_cast<AuthCheckedPhone::AuthCheckedPhoneClassType>(type));
    switch(type) {
    case AuthCheckedPhone::typeAuthCheckedPhone: {
        bool m_phone_registered;
        stream >> m_phone_registered;
        item.setPhoneRegistered(m_phone_registered);
    }
        break;
    }
    return stream;
}

QDebug operator<<(QDebug debug,  const AuthCheckedPhone &item) {
    QDebugStateSaver saver(debug);
    Q_UNUSED(saver)
    debug.nospace() << "Telegram.AuthCheckedPhone(";
    switch(item.classType()) {
    case AuthCheckedPhone::typeAuthCheckedPhone:
        debug.nospace() << "classType: typeAuthCheckedPhone";
        debug.nospace() << ", phoneRegistered: " << item.phoneRegistered();
        break;
    }
    debug.nospace() << ")";
    return debug;
}

