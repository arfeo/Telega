// This file generated by libqtelegram-code-generator.
// You can download it from: https://github.com/Aseman-Land/libqtelegram-code-generator
// DO NOT EDIT THIS FILE BY HAND -- YOUR CHANGES WILL BE OVERWRITTEN

#include "contactstoppeers.h"
#include "core/inboundpkt.h"
#include "core/outboundpkt.h"
#include "../coretypes.h"

#include <QDataStream>

ContactsTopPeers::ContactsTopPeers(ContactsTopPeersClassType classType, InboundPkt *in) :
    m_classType(classType)
{
    if(in) fetch(in);
}

ContactsTopPeers::ContactsTopPeers(InboundPkt *in) :
    m_classType(typeContactsTopPeersNotModified)
{
    fetch(in);
}

ContactsTopPeers::ContactsTopPeers(const ContactsTopPeers &another) :
    TelegramTypeObject(),
    m_classType(typeContactsTopPeersNotModified)
{
    operator=(another);
}

ContactsTopPeers::ContactsTopPeers(const Null &null) :
    TelegramTypeObject(null),
    m_classType(typeContactsTopPeersNotModified)
{
}

ContactsTopPeers::~ContactsTopPeers() {

}

void ContactsTopPeers::setCategories(const QList<TopPeerCategoryPeers> &categories) {
    m_categories = categories;
}

QList<TopPeerCategoryPeers> ContactsTopPeers::categories() const {
    return m_categories;
}

void ContactsTopPeers::setChats(const QList<Chat> &chats) {
    m_chats = chats;
}

QList<Chat> ContactsTopPeers::chats() const {
    return m_chats;
}

void ContactsTopPeers::setUsers(const QList<User> &users) {
    m_users = users;
}

QList<User> ContactsTopPeers::users() const {
    return m_users;
}

bool ContactsTopPeers::operator ==(const ContactsTopPeers &b) const {
    return m_classType == b.m_classType &&
           m_categories == b.m_categories &&
           m_chats == b.m_chats &&
           m_users == b.m_users;
}

ContactsTopPeers &ContactsTopPeers::operator =(const ContactsTopPeers &b) {
    m_classType = b.m_classType;
    m_categories = b.m_categories;
    m_chats = b.m_chats;
    m_users = b.m_users;
    setNull(b.isNull());
    return *this;
}

void ContactsTopPeers::setClassType(ContactsTopPeers::ContactsTopPeersClassType classType) {
    m_classType = classType;
}

ContactsTopPeers::ContactsTopPeersClassType ContactsTopPeers::classType() const {
    return m_classType;
}

bool ContactsTopPeers::fetch(InboundPkt *in) {
    LQTG_FETCH_LOG;
    int x = in->fetchInt();
    switch(x) {
    case typeContactsTopPeersNotModified: {
        m_classType = static_cast<ContactsTopPeersClassType>(x);
        return true;
    }
        break;

    case typeContactsTopPeers: {
        if(in->fetchInt() != (qint32)CoreTypes::typeVector) return false;
        qint32 m_categories_length = in->fetchInt();
        m_categories.clear();
        for (qint32 i = 0; i < m_categories_length; i++) {
            TopPeerCategoryPeers type;
            type.fetch(in);
            m_categories.append(type);
        }
        if(in->fetchInt() != (qint32)CoreTypes::typeVector) return false;
        qint32 m_chats_length = in->fetchInt();
        m_chats.clear();
        for (qint32 i = 0; i < m_chats_length; i++) {
            Chat type;
            type.fetch(in);
            m_chats.append(type);
        }
        if(in->fetchInt() != (qint32)CoreTypes::typeVector) return false;
        qint32 m_users_length = in->fetchInt();
        m_users.clear();
        for (qint32 i = 0; i < m_users_length; i++) {
            User type;
            type.fetch(in);
            m_users.append(type);
        }
        m_classType = static_cast<ContactsTopPeersClassType>(x);
        return true;
    }
        break;

    default:
        LQTG_FETCH_ASSERT;
        return false;
    }
}

bool ContactsTopPeers::push(OutboundPkt *out) const {
    out->appendInt(m_classType);
    switch(m_classType) {
    case typeContactsTopPeersNotModified: {
        return true;
    }
        break;

    case typeContactsTopPeers: {
        out->appendInt(CoreTypes::typeVector);
        out->appendInt(m_categories.count());
        for (qint32 i = 0; i < m_categories.count(); i++) {
            m_categories[i].push(out);
        }
        out->appendInt(CoreTypes::typeVector);
        out->appendInt(m_chats.count());
        for (qint32 i = 0; i < m_chats.count(); i++) {
            m_chats[i].push(out);
        }
        out->appendInt(CoreTypes::typeVector);
        out->appendInt(m_users.count());
        for (qint32 i = 0; i < m_users.count(); i++) {
            m_users[i].push(out);
        }
        return true;
    }
        break;

    default:
        return false;
    }
}

QMap<QString, QVariant> ContactsTopPeers::toMap() const {
    QMap<QString, QVariant> result;
    switch(static_cast<int>(m_classType)) {
    case typeContactsTopPeersNotModified: {
        result["classType"] = "ContactsTopPeers::typeContactsTopPeersNotModified";
        return result;
    }
        break;

    case typeContactsTopPeers: {
        result["classType"] = "ContactsTopPeers::typeContactsTopPeers";
        QList<QVariant> _categories;
        //for(const TopPeerCategoryPeers &m__type: m_categories)
        for(int i = 0; i < m_categories.size(); ++i) {
            const TopPeerCategoryPeers &m__type = m_categories[i];
            if( !m__type.isNull() ) _categories << m__type.toMap();
        }
        result["categories"] = _categories;
        QList<QVariant> _chats;
        //for(const Chat &m__type: m_chats)
        for(int i = 0; i < m_chats.size(); ++i) {
            const Chat &m__type = m_chats[i];
            if( !m__type.isNull() ) _chats << m__type.toMap();
        }
        result["chats"] = _chats;
        QList<QVariant> _users;
        //for(const User &m__type: m_users)
        for(int i = 0; i < m_users.size(); ++i) {
            const User &m__type = m_users[i];
            if( !m__type.isNull() ) _users << m__type.toMap();
        }
        result["users"] = _users;
        return result;
    }
        break;

    default:
        return result;
    }
}

ContactsTopPeers ContactsTopPeers::fromMap(const QMap<QString, QVariant> &map) {
    ContactsTopPeers result;
    if(map.value("classType").toString() == "ContactsTopPeers::typeContactsTopPeersNotModified") {
        result.setClassType(typeContactsTopPeersNotModified);
        return result;
    }
    if(map.value("classType").toString() == "ContactsTopPeers::typeContactsTopPeers") {
        result.setClassType(typeContactsTopPeers);
        QList<QVariant> map_categories = map["categories"].toList();
        QList<TopPeerCategoryPeers> _categories;
        //for(const QVariant &var: map_categories)
        for(int i = 0; i < map_categories.size(); ++i) {
            const QVariant &var = map_categories[i];
            _categories << TopPeerCategoryPeers::fromMap(var.toMap());
        }
        result.setCategories(_categories);
        QList<QVariant> map_chats = map["chats"].toList();
        QList<Chat> _chats;
        //for(const QVariant &var: map_chats)
        for(int i = 0; i < map_chats.size(); ++i) {
            const QVariant &var = map_chats[i];
            _chats << Chat::fromMap(var.toMap());
        }
        result.setChats(_chats);
        QList<QVariant> map_users = map["users"].toList();
        QList<User> _users;
        //for(const QVariant &var: map_users)
        for(int i = 0; i < map_users.size(); ++i) {
            const QVariant &var = map_users[i];
            _users << User::fromMap(var.toMap());
        }
        result.setUsers(_users);
        return result;
    }
    return result;
}

ContactsTopPeers ContactsTopPeers::fromJson(const QString &json) {
    return ContactsTopPeers::fromMap(QJsonDocument::fromJson(json.toUtf8()).toVariant().toMap());
}

QByteArray ContactsTopPeers::getHash(QCryptographicHash::Algorithm alg) const {
    QByteArray data;
    QDataStream str(&data, QIODevice::WriteOnly);
    str << *this;
    return QCryptographicHash::hash(data, alg);
}

QDataStream &operator<<(QDataStream &stream, const ContactsTopPeers &item) {
    stream << static_cast<uint>(item.classType());
    switch(item.classType()) {
    case ContactsTopPeers::typeContactsTopPeersNotModified:

        break;
    case ContactsTopPeers::typeContactsTopPeers:
        stream << item.categories();
        stream << item.chats();
        stream << item.users();
        break;
    }
    return stream;
}

QDataStream &operator>>(QDataStream &stream, ContactsTopPeers &item) {
    uint type = 0;
    stream >> type;
    item.setClassType(static_cast<ContactsTopPeers::ContactsTopPeersClassType>(type));
    switch(type) {
    case ContactsTopPeers::typeContactsTopPeersNotModified: {

    }
        break;
    case ContactsTopPeers::typeContactsTopPeers: {
        QList<TopPeerCategoryPeers> m_categories;
        stream >> m_categories;
        item.setCategories(m_categories);
        QList<Chat> m_chats;
        stream >> m_chats;
        item.setChats(m_chats);
        QList<User> m_users;
        stream >> m_users;
        item.setUsers(m_users);
    }
        break;
    }
    return stream;
}

/*QDebug operator<<(QDebug debug,  const ContactsTopPeers &item) {
    QDebugStateSaver saver(debug);
    Q_UNUSED(saver)
    debug.nospace() << "Telegram.ContactsTopPeers(";
    switch(item.classType()) {
    case ContactsTopPeers::typeContactsTopPeersNotModified:
        debug.nospace() << "classType: typeContactsTopPeersNotModified";
        break;
    case ContactsTopPeers::typeContactsTopPeers:
        debug.nospace() << "classType: typeContactsTopPeers";
        debug.nospace() << ", categories: " << item.categories();
        debug.nospace() << ", chats: " << item.chats();
        debug.nospace() << ", users: " << item.users();
        break;
    }
    debug.nospace() << ")";
    return debug;
}
*/
