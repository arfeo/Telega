// This file generated by libqtelegram-code-generator.
// You can download it from: https://github.com/Aseman-Land/libqtelegram-code-generator
// DO NOT EDIT THIS FILE BY HAND -- YOUR CHANGES WILL BE OVERWRITTEN

#include "inputencryptedchat.h"
#include "core/inboundpkt.h"
#include "core/outboundpkt.h"
#include "../coretypes.h"

#include <QDataStream>

InputEncryptedChat::InputEncryptedChat(InputEncryptedChatClassType classType, InboundPkt *in) :
    m_accessHash(0),
    m_chatId(0),
    m_classType(classType)
{
    if(in) fetch(in);
}

InputEncryptedChat::InputEncryptedChat(InboundPkt *in) :
    m_accessHash(0),
    m_chatId(0),
    m_classType(typeInputEncryptedChat)
{
    fetch(in);
}

InputEncryptedChat::InputEncryptedChat(const InputEncryptedChat &another) :
    TelegramTypeObject(),
    m_accessHash(0),
    m_chatId(0),
    m_classType(typeInputEncryptedChat)
{
    operator=(another);
}

InputEncryptedChat::InputEncryptedChat(const Null &null) :
    TelegramTypeObject(null),
    m_accessHash(0),
    m_chatId(0),
    m_classType(typeInputEncryptedChat)
{
}

InputEncryptedChat::~InputEncryptedChat() {
    
}

void InputEncryptedChat::setAccessHash(qint64 accessHash) {
    m_accessHash = accessHash;
}

qint64 InputEncryptedChat::accessHash() const {
    return m_accessHash;
}

void InputEncryptedChat::setChatId(qint32 chatId) {
    m_chatId = chatId;
}

qint32 InputEncryptedChat::chatId() const {
    return m_chatId;
}

bool InputEncryptedChat::operator ==(const InputEncryptedChat &b) const {
    return m_classType == b.m_classType &&
           m_accessHash == b.m_accessHash &&
           m_chatId == b.m_chatId;
}

InputEncryptedChat &InputEncryptedChat::operator =(const InputEncryptedChat &b) {
    m_classType = b.m_classType;
    m_accessHash = b.m_accessHash;
    m_chatId = b.m_chatId;
    setNull(b.isNull());
    return *this;
}

void InputEncryptedChat::setClassType(InputEncryptedChat::InputEncryptedChatClassType classType) {
    m_classType = classType;
}

InputEncryptedChat::InputEncryptedChatClassType InputEncryptedChat::classType() const {
    return m_classType;
}

bool InputEncryptedChat::fetch(InboundPkt *in) {
    LQTG_FETCH_LOG;
    int x = in->fetchInt();
    switch(x) {
    case typeInputEncryptedChat: {
        m_chatId = in->fetchInt();
        m_accessHash = in->fetchLong();
        m_classType = static_cast<InputEncryptedChatClassType>(x);
        return true;
    }
        break;
    
    default:
        LQTG_FETCH_ASSERT;
        return false;
    }
}

bool InputEncryptedChat::push(OutboundPkt *out) const {
    out->appendInt(m_classType);
    switch(m_classType) {
    case typeInputEncryptedChat: {
        out->appendInt(m_chatId);
        out->appendLong(m_accessHash);
        return true;
    }
        break;
    
    default:
        return false;
    }
}

QMap<QString, QVariant> InputEncryptedChat::toMap() const {
    QMap<QString, QVariant> result;
    switch(static_cast<int>(m_classType)) {
    case typeInputEncryptedChat: {
        result["classType"] = "InputEncryptedChat::typeInputEncryptedChat";
        if( chatId() ) result["chatId"] = QString::number(chatId());
        if( accessHash() ) result["accessHash"] = QString::number(accessHash());
        return result;
    }
        break;
    
    default:
        return result;
    }
}

InputEncryptedChat InputEncryptedChat::fromMap(const QMap<QString, QVariant> &map) {
    InputEncryptedChat result;
    if(map.value("classType").toString() == "InputEncryptedChat::typeInputEncryptedChat") {
        result.setClassType(typeInputEncryptedChat);
        QVariant _chatId_var = map.value("chatId");
        if( !_chatId_var.isNull() ) {
            _chatId_var.convert( QVariant::nameToType("qint32") );
            result.setChatId( _chatId_var.value<qint32>() );
        }
        
        QVariant _accessHash_var = map.value("accessHash");
        if( !_accessHash_var.isNull() ) {
            _accessHash_var.convert( QVariant::nameToType("qint64") );
            result.setAccessHash( _accessHash_var.value<qint64>() );
        }
        
        return result;
    }
    return result;
}

InputEncryptedChat InputEncryptedChat::fromJson(const QString &json) {
    return InputEncryptedChat::fromMap(QJsonDocument::fromJson(json.toUtf8()).toVariant().toMap());
}

QByteArray InputEncryptedChat::getHash(QCryptographicHash::Algorithm alg) const {
    QByteArray data;
    QDataStream str(&data, QIODevice::WriteOnly);
    str << *this;
    return QCryptographicHash::hash(data, alg);
}

QDataStream &operator<<(QDataStream &stream, const InputEncryptedChat &item) {
    stream << static_cast<uint>(item.classType());
    switch(item.classType()) {
    case InputEncryptedChat::typeInputEncryptedChat:
        stream << item.chatId();
        stream << item.accessHash();
        break;
    }
    return stream;
}

QDataStream &operator>>(QDataStream &stream, InputEncryptedChat &item) {
    uint type = 0;
    stream >> type;
    item.setClassType(static_cast<InputEncryptedChat::InputEncryptedChatClassType>(type));
    switch(type) {
    case InputEncryptedChat::typeInputEncryptedChat: {
        qint32 m_chat_id;
        stream >> m_chat_id;
        item.setChatId(m_chat_id);
        qint64 m_access_hash;
        stream >> m_access_hash;
        item.setAccessHash(m_access_hash);
    }
        break;
    }
    return stream;
}

QDebug operator<<(QDebug debug,  const InputEncryptedChat &item) {
    QDebugStateSaver saver(debug);
    Q_UNUSED(saver)
    debug.nospace() << "Telegram.InputEncryptedChat(";
    switch(item.classType()) {
    case InputEncryptedChat::typeInputEncryptedChat:
        debug.nospace() << "classType: typeInputEncryptedChat";
        debug.nospace() << ", chatId: " << item.chatId();
        debug.nospace() << ", accessHash: " << item.accessHash();
        break;
    }
    debug.nospace() << ")";
    return debug;
}

