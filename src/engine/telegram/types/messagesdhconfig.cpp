// This file generated by libqtelegram-code-generator.
// You can download it from: https://github.com/Aseman-Land/libqtelegram-code-generator
// DO NOT EDIT THIS FILE BY HAND -- YOUR CHANGES WILL BE OVERWRITTEN

#include "messagesdhconfig.h"
#include "core/inboundpkt.h"
#include "core/outboundpkt.h"
#include "../coretypes.h"

#include <QDataStream>

MessagesDhConfig::MessagesDhConfig(MessagesDhConfigClassType classType, InboundPkt *in) :
    m_g(0),
    m_version(0),
    m_classType(classType)
{
    if(in) fetch(in);
}

MessagesDhConfig::MessagesDhConfig(InboundPkt *in) :
    m_g(0),
    m_version(0),
    m_classType(typeMessagesDhConfigNotModified)
{
    fetch(in);
}

MessagesDhConfig::MessagesDhConfig(const MessagesDhConfig &another) :
    TelegramTypeObject(),
    m_g(0),
    m_version(0),
    m_classType(typeMessagesDhConfigNotModified)
{
    operator=(another);
}

MessagesDhConfig::MessagesDhConfig(const Null &null) :
    TelegramTypeObject(null),
    m_g(0),
    m_version(0),
    m_classType(typeMessagesDhConfigNotModified)
{
}

MessagesDhConfig::~MessagesDhConfig() {

}

void MessagesDhConfig::setG(qint32 g) {
    m_g = g;
}

qint32 MessagesDhConfig::g() const {
    return m_g;
}

void MessagesDhConfig::setP(const QByteArray &p) {
    m_p = p;
}

QByteArray MessagesDhConfig::p() const {
    return m_p;
}

void MessagesDhConfig::setRandom(const QByteArray &random) {
    m_random = random;
}

QByteArray MessagesDhConfig::random() const {
    return m_random;
}

void MessagesDhConfig::setVersion(qint32 version) {
    m_version = version;
}

qint32 MessagesDhConfig::version() const {
    return m_version;
}

bool MessagesDhConfig::operator ==(const MessagesDhConfig &b) const {
    return m_classType == b.m_classType &&
           m_g == b.m_g &&
           m_p == b.m_p &&
           m_random == b.m_random &&
           m_version == b.m_version;
}

MessagesDhConfig &MessagesDhConfig::operator =(const MessagesDhConfig &b) {
    m_classType = b.m_classType;
    m_g = b.m_g;
    m_p = b.m_p;
    m_random = b.m_random;
    m_version = b.m_version;
    setNull(b.isNull());
    return *this;
}

void MessagesDhConfig::setClassType(MessagesDhConfig::MessagesDhConfigClassType classType) {
    m_classType = classType;
}

MessagesDhConfig::MessagesDhConfigClassType MessagesDhConfig::classType() const {
    return m_classType;
}

bool MessagesDhConfig::fetch(InboundPkt *in) {
    LQTG_FETCH_LOG;
    int x = in->fetchInt();
    switch(x) {
    case typeMessagesDhConfigNotModified: {
        m_random = in->fetchBytes();
        m_classType = static_cast<MessagesDhConfigClassType>(x);
        return true;
    }
        break;

    case typeMessagesDhConfig: {
        m_g = in->fetchInt();
        m_p = in->fetchBytes();
        m_version = in->fetchInt();
        m_random = in->fetchBytes();
        m_classType = static_cast<MessagesDhConfigClassType>(x);
        return true;
    }
        break;

    default:
        LQTG_FETCH_ASSERT;
        return false;
    }
}

bool MessagesDhConfig::push(OutboundPkt *out) const {
    out->appendInt(m_classType);
    switch(m_classType) {
    case typeMessagesDhConfigNotModified: {
        out->appendBytes(m_random);
        return true;
    }
        break;

    case typeMessagesDhConfig: {
        out->appendInt(m_g);
        out->appendBytes(m_p);
        out->appendInt(m_version);
        out->appendBytes(m_random);
        return true;
    }
        break;

    default:
        return false;
    }
}

QMap<QString, QVariant> MessagesDhConfig::toMap() const {
    QMap<QString, QVariant> result;
    switch(static_cast<int>(m_classType)) {
    case typeMessagesDhConfigNotModified: {
        result["classType"] = "MessagesDhConfig::typeMessagesDhConfigNotModified";
        if( !m_random.isEmpty() ) result["random"] = QVariant::fromValue<QByteArray>(m_random);
        return result;
    }
        break;

    case typeMessagesDhConfig: {
        result["classType"] = "MessagesDhConfig::typeMessagesDhConfig";
        if( g() ) result["g"] = QString::number(g());
        if( !m_p.isEmpty() ) result["p"] = QVariant::fromValue<QByteArray>(m_p);
        if( version() ) result["version"] = QString::number(version());
        if( !m_random.isEmpty() ) result["random"] = QVariant::fromValue<QByteArray>(m_random);
        return result;
    }
        break;

    default:
        return result;
    }
}

MessagesDhConfig MessagesDhConfig::fromMap(const QMap<QString, QVariant> &map) {
    MessagesDhConfig result;
    if(map.value("classType").toString() == "MessagesDhConfig::typeMessagesDhConfigNotModified") {
        result.setClassType(typeMessagesDhConfigNotModified);
        QVariant _random_var = map.value("random");
        if( !_random_var.isNull() ) {
            _random_var.convert( QVariant::nameToType("QByteArray") );
            result.setRandom( _random_var.value<QByteArray>() );
        }

        return result;
    }
    if(map.value("classType").toString() == "MessagesDhConfig::typeMessagesDhConfig") {
        result.setClassType(typeMessagesDhConfig);
        QVariant _g_var = map.value("g");
        if( !_g_var.isNull() ) {
            _g_var.convert( QVariant::nameToType("qint32") );
            result.setG( _g_var.value<qint32>() );
        }

        QVariant _p_var = map.value("p");
        if( !_p_var.isNull() ) {
            _p_var.convert( QVariant::nameToType("QByteArray") );
            result.setP( _p_var.value<QByteArray>() );
        }

        QVariant _version_var = map.value("version");
        if( !_version_var.isNull() ) {
            _version_var.convert( QVariant::nameToType("qint32") );
            result.setVersion( _version_var.value<qint32>() );
        }

        QVariant _random_var = map.value("random");
        if( !_random_var.isNull() ) {
            _random_var.convert( QVariant::nameToType("QByteArray") );
            result.setRandom( _random_var.value<QByteArray>() );
        }

        return result;
    }
    return result;
}

MessagesDhConfig MessagesDhConfig::fromJson(const QString &json) {
    return MessagesDhConfig::fromMap(QJsonDocument::fromJson(json.toUtf8()).toVariant().toMap());
}

QByteArray MessagesDhConfig::getHash(QCryptographicHash::Algorithm alg) const {
    QByteArray data;
    QDataStream str(&data, QIODevice::WriteOnly);
    str << *this;
    return QCryptographicHash::hash(data, alg);
}

QDataStream &operator<<(QDataStream &stream, const MessagesDhConfig &item) {
    stream << static_cast<uint>(item.classType());
    switch(item.classType()) {
    case MessagesDhConfig::typeMessagesDhConfigNotModified:
        stream << item.random();
        break;
    case MessagesDhConfig::typeMessagesDhConfig:
        stream << item.g();
        stream << item.p();
        stream << item.version();
        stream << item.random();
        break;
    }
    return stream;
}

QDataStream &operator>>(QDataStream &stream, MessagesDhConfig &item) {
    uint type = 0;
    stream >> type;
    item.setClassType(static_cast<MessagesDhConfig::MessagesDhConfigClassType>(type));
    switch(type) {
    case MessagesDhConfig::typeMessagesDhConfigNotModified: {
        QByteArray m_random;
        stream >> m_random;
        item.setRandom(m_random);
    }
        break;
    case MessagesDhConfig::typeMessagesDhConfig: {
        qint32 m_g;
        stream >> m_g;
        item.setG(m_g);
        QByteArray m_p;
        stream >> m_p;
        item.setP(m_p);
        qint32 m_version;
        stream >> m_version;
        item.setVersion(m_version);
        QByteArray m_random;
        stream >> m_random;
        item.setRandom(m_random);
    }
        break;
    }
    return stream;
}

/*QDebug operator<<(QDebug debug,  const MessagesDhConfig &item) {
    QDebugStateSaver saver(debug);
    Q_UNUSED(saver)
    debug.nospace() << "Telegram.MessagesDhConfig(";
    switch(item.classType()) {
    case MessagesDhConfig::typeMessagesDhConfigNotModified:
        debug.nospace() << "classType: typeMessagesDhConfigNotModified";
        debug.nospace() << ", random: " << item.random();
        break;
    case MessagesDhConfig::typeMessagesDhConfig:
        debug.nospace() << "classType: typeMessagesDhConfig";
        debug.nospace() << ", g: " << item.g();
        debug.nospace() << ", p: " << item.p();
        debug.nospace() << ", version: " << item.version();
        debug.nospace() << ", random: " << item.random();
        break;
    }
    debug.nospace() << ")";
    return debug;
}
*/
